// Crear un arreglo de nombres
let nombres = ["Carlos", "Ana", "Miguel", "Sara", "Laura"];

// Definir el nombre a buscar
let nombreABuscar = "Miguel";

// Verificar si el nombre está presente en el arreglo
if (nombres.includes(nombreABuscar)) {
    console.log(`${nombreABuscar} está presente en el arreglo.`);
} else {
    console.log(`${nombreABuscar} no está presente en el arreglo.`);
}
